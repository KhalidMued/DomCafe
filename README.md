# DŌM Home Café OS

Private home café ordering and control system for DŌM.

## Phase

Phase 0 — Foundation.

## Local development

```bash
cp env.example .env
docker compose up -d --build
```

Open: <http://localhost:11080>

## Rules

Every task starts by reading `AGENT.md`. Work happens on feature branches and PRs into `main`; do not push directly to `main`.
